valid property name text, numbers, _, '

updating templates
- deleting a property: including existing cards y/n
- updating a property:
	- if type, check conversion, convert and re-validate
	- if validation, re-validate
- revalidation: flag cards that do not comply
Deleting a template

test json API
////////////////////////////////
curl -i -H "Accept: application/json" -H "Content-Type: application/json" -X PUT -d "{\"title\":\"abc"\"}" http://localhost:3000/cards/7.json
////////////////////////////////


Creating cards
	- w/properties
updating card properties
card: template_id, title, properties

template validations:
 - mandatory "mandatory;"
 - unique "unique;"
 - String: max-length:nn; 
 			min length:nn;
 			email;
 			telno;
 			regex:rrr;
 			<:ss; <=:ss; >:ss; >=:ss;
 - Date: before:dd; after:dd;
 - Time: before:tt; after:tt;
 - DateTime: before, after
 - Number: above:nn; below:nn;
 + List: included-in:[l1,l2,l3,..]
 - Reference: Link:{template,property}
